# Dynosam NN module

- Much of the code is taken from visual-perception-engine.

# Install
- python3 -m pip install "ultralytics==8.2.55" "numpy<2.0" "opencv-python<5.0"
- sudo apt install python3-pybind11
