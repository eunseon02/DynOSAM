#!/bin/bash

--use_byte_tracker=false

--init_object_pose_from_gt=true
--use_frontend_logger=true

--semantic_mask_step_size=10

#we turn off for cluster as there is some problem with the OF I think?
--use_propogate_mask=true

# we turn off for OMD as there is some problem
--use_dynamic_track=false

--log_projected_masks=false

--refine_motion_estimate=false
--refine_with_optical_flow=false

# kitti values
--shrink_row=25
--shrink_col=50

# --shrink_row=0
# --shrink_col=0
